[Emulation]
# Speed up hw_emu by disabling waveform generation.
#
# XRT supports `debug_mode` (off|batch|gui|gdb). Some examples/documentation also
# reference `launch_waveform`; keep both for compatibility across tool versions.
debug_mode=off
launch_waveform=off
